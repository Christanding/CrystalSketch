# CrystalSketch

CrystalSketch 是一款在浏览器中使用的晶体结构可视化与制图工具。打开 POSCAR、CONTCAR 或 CIF 等结构文件后，即可查看三维结构、调整配色和材质、测量原子间的距离与夹角，并导出用于论文、报告或演示的结构图。

**[直接在线使用 CrystalSketch](https://christanding.github.io/CrystalSketch/)**，无需安装。

- **美观**：内置多套元素配色和材质，支持调节光照，展示原子、化学键、配位多面体与晶轴。
- **易用**：在浏览器中完成结构加载、预览与图片导出，支持多文件切换和双视图对比。
- **可靠**：保留原始结构文件不变；POSCAR / CONTCAR 可在浏览器中解析，其他格式读取与对称性分析由 pymatgen 支持。
- **高效**：针对多原子结构优化显示与交互，实际流畅度取决于结构复杂度和设备配置。
- **灵活**：颜色、半径、材质、透明度、视角和导出参数均可调整，支持 PNG、JPG 和 PDF 图片导出，也可创建模型副本进行编辑并导出 POSCAR。

## 在线使用

打开 [网页版](https://christanding.github.io/CrystalSketch/)，点击“打开”选择结构文件，即可开始使用。

三维显示与图片导出在浏览器中完成。在线版的 CIF 等非 VASP 文件解析，以及对称性分析，会将结构内容发送到云端服务；如需完全在本机处理，请使用下方的本地安装版。

## 本地安装

支持 macOS、Windows 和 Linux，使用支持 WebGL 的现代浏览器。

### 下载压缩包

前往 [GitHub Releases](https://github.com/Christanding/CrystalSketch/releases/latest)，在 **Assets** 中下载 **CrystalSketch.zip**，或[直接下载最新安装包](https://github.com/Christanding/CrystalSketch/releases/latest/download/CrystalSketch.zip)。不要将 GitHub 自动生成的 `Source code (zip)` 当作安装包。

解压后，在 `CrystalSketch` 文件夹中打开终端，按系统执行一条安装命令。

macOS / Linux：

```sh
bash install.sh
```

Windows（PowerShell）：

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1
```

首次安装需要联网，安装程序会自动准备 Python 和所需依赖，无需手动安装开发工具或编译项目。安装完成后重新打开终端即可使用，不需要保留源码文件夹。

Windows 安装器会自动将 `Crystal` 的命令目录保存到用户 PATH，并在新 PowerShell 进程中验证。安装后请完全退出 Windows Terminal 再重新打开一次；之后直接输入 `Crystal` 即可，不需要反复修复。

### 从 GitHub 仓库安装（推荐）

Windows 如果尚未安装 Git，先执行：

```powershell
winget install --id Git.Git -e --source winget
```

安装完成后重新打开终端，再执行下面的安装命令。

已安装 Git 时，也可以用一条命令拉取仓库并安装。

macOS / Linux：

```sh
git clone https://github.com/Christanding/CrystalSketch.git && cd CrystalSketch && bash scripts/install.sh
```

Windows（PowerShell）：

```powershell
git clone https://github.com/Christanding/CrystalSketch.git; if ($LASTEXITCODE -eq 0) { Set-Location CrystalSketch; powershell -ExecutionPolicy Bypass -File .\scripts\install.ps1 }
```

脚本会自动准备所需工具、构建并安装当前仓库版本，不需要分别运行网页与后台服务。安装完成后重新打开终端即可。

## 启动与使用

直接打开终端，输入以下命令即可，无需进入项目文件夹：

```sh
Crystal
```

浏览器会自动打开 CrystalSketch。如果没有自动打开，点击终端显示的本地网址即可，无需指定端口或另开服务。

1. 点击“打开”，选择晶体结构文件。
2. 旋转、缩放结构，按需要调整配色、材质、光照和显示内容。
3. 在“导出”中设置图片参数并保存；如需修改结构，先在“建模”中创建模型副本，完成修改后再导出 POSCAR。

使用期间保持启动终端运行。结束后，在该终端按 `Ctrl+C` 停止程序；下次使用只需再次执行启动命令，无需重新安装。

## 渲染与出图

右侧面板分为“设置、物体、渲染、建模”。“设置”保留语言、主题、交互和更新；光照、画面、预览质量及 FPS 等选项统一放在“渲染”中。元素配色和材质仍在左侧“风格”中选择。

- **实时**适合旋转、选取、测量和建模。可启用“接触阴影”（AO），增强原子、键及多面体相接处的层次；关闭时不加载 AO 渲染资源。含半透明或透射材质时，AO 只作用于不透明结构，透明壳层不遮挡、不单独接收 AO；最终保留原生多层透明混合、透射和 PNG alpha，不对混合后的颜色再次做色调映射。
- **高质量**使用渐进式路径追踪，须明确选用物理陶瓷、哑光、金属或玻璃等 PBR 材质，不会自动替换原来的插画材质。视图移动时显示实时预览，静止后重新采样；前几次采样不直接显示，之后进行保边降噪。可以暂停、继续或重新渲染，完成后停止采样。
- **布光与物理参数**彼此独立：在“渲染”选择布光并调节曝光；在“风格”调节粗糙度、金属度、清漆和透射。原有材质默认、柔光箱、侧光和轮廓光保持原效果。新增均匀柔光（四面宽光板）、顶光（上方主光与弱前补光）、双条形光（两侧窄高灯与弱前补光）和伦勃朗式（侧上主光与弱对侧补光）。选择新增预设会同步主光方向，之后仍可手动调整方向与整体布光旋转；面光源和环境反射使用同一灯组朝向，实时预览、路径追踪和导出共用这套定义，不改变材质、配色或图像尺寸。伦勃朗式表示明暗关系，不保证晶体产生人像中的三角光斑。
- **新增 PBR 材质**：高釉陶瓷（清漆釉层）、亮面塑料（光滑非金属反射）、橡胶哑光（高粗糙度、弱高光）、镜面金属（低粗糙度金属反射）、磨砂玻璃（粗糙透射）、丝绒柔光（绒面边缘散射）和珠光涂层（薄膜干涉加清漆）。七项均可用于实时、路径追踪与导出，沿用现有元素色、组件不透明度和几何尺寸；切换材质会重置此前手动调整的物理参数，但保留渲染模式和布光。缩略图按需使用同一物理材质和柔光箱生成。
- **PBR 配色**：在“风格 → 配色方案”中提供珐琅蓝橙、孔雀赤陶、紫晶香槟、胭脂青瓷、合金冷暖和薄荷紫晶。前四套突出不透明材质的底色与高光；合金冷暖采用较亮的金银色，薄荷紫晶采用适合透射的浅底色。这些是视觉配色，不是实测金属反射率。每套包含现有全部 97 个元素/占位符条目（包括 D、XX），并为常见多元结构预先安排区分色；元素颜色固定，不随结构组成或加载顺序重新分配。Cu/Na 与 I/Cl 保留确认过的核心双色，切换配色不改变材质、光追模式、灯光、尺寸或导出设置。原有配色和启动默认保持不变。
- **导出**沿用当前渲染模式，支持 PNG、JPG 和 PDF。高质量导出使用更高采样数，提供进度与取消；测量标注单独合成，透明 PNG 保留透明背景。

路径追踪导出会分批等待 GPU 完成，避免复杂透射场景堆积绘制指令；最终显卡同步通过后才生成图像。同步失败或上下文丢失会明确报错，不会把空图当作导出成功，也不会自动降低采样或分辨率。

透明累积始终读取最新写入的缓冲区，避免偶数次采样显示上一轮结果；预览与导出使用同一修复，不改变采样数或混合公式。

采样与显示合成分别调度：正式导出累积完成后只做一次最终降噪和合成；交互预览最多按 30 Hz 合成中间画面，首次可显示的完整采样和最终结果不受这一节流限制。暂停后的曝光调整仍即时生效，取消及最终 GPU 完成确认保留。

预览在相机惯性结束时清除残余控制量，避免进度更新再次唤醒微小运动并反复清零采样。旋转只重新采样；缩放造成的边线半径变化原地更新解析图元和 BVH 包围盒。几何兼容的材质、配色、不透明度及布光变化复用现有会话；网格拓扑或三角几何不兼容时仍走独立新会话。预览最终完成也须经过 GPU 同步确认，保留原来的采样、反弹、降噪和导出参数。

纯材质、配色及正不透明度调整也复用 CPU 场景和网格，双色键只在端点颜色改变时拆分共享颜色几何；几何未变时跳过 BVH 包围盒重算。几何、可见性或透明度跨零变化仍保留原有重建路径，所有影响画面的修改都清空旧采样。

未启用的低分辨率备用渲染器不分配 GPU 资源。依赖原有的动态低分辨率预览功能仍可按需启用，首次使用才创建，随后随整个会话释放；当前预览与导出不使用这条备用路径。

路径追踪自动把标准原子球、化学键及晶胞/多面体边线的圆柱交给解析图元 BVH：直接求数学曲面的交点，保留原子位置、半径、键长、中点双色以及原有开口/端帽，不再对这些图元逐三角形求交。主射线、反弹、灯光和环境阴影共用同一混合求交入口；多面体表面及不支持的变形、纹理等继续使用原三角网格。仅当可见网格全部已纳入解析路径时，才跳过空的三角 BVH 查询；权重为零的珠光/绒面项不再计算，非零项仍使用原完整模型。材质、颜色、反弹上限、采样数、图像尺寸和超采样不变。解析曲面的轮廓比离散网格更平滑，因此不保证与旧网格逐像素一致；高精度网格参考用于验证几何和外观。

这条加速路径使用 Windows/macOS 共用的 WebGL2/GLSL，不依赖 Metal、CUDA 或硬件光追。准备阶段先以单像素执行真实着色器及 GPU 同步检查；若可选解析内核不兼容，会恢复完整三角场景和原着色器，取消或图形上下文丢失不会触发重复构建。具体提速随材质、几何和显卡而变，不能由三角形减少比例推算。

绒面与珠光是表面着色模型，不增加真实纤维或多层珍珠结构；珠光使用均匀薄膜，未配置厚度贴图。磨砂透射采用当前渲染器的粗糙透射近似。实时与路径追踪共用材质参数，但多次反射、透射和面光源高光的计算不同，不保证逐像素一致。

路径追踪需要 WebGL2、浮点渲染缓冲和 Web Worker，耗时取决于可见几何、图像尺寸和设备。交互视图的光追预览限制分辨率以控制显存；图片预览和实际导出均保留用户设定的尺寸、网格质量与超采样，共用原有的 1600 万内部像素预算（包括超采样，例如 2000×2000、2×超采样）。光追仍受设备 `MAX_TEXTURE_SIZE`、`MAX_RENDERBUFFER_SIZE` 和可用显存限制，几何最多 150 万个三角形和 12000 个网格。1600 万是准入上限，并不保证每台设备都能分配足够显存；失败或不支持时会明确提示，不会自动降低分辨率或更换材质，可返回实时模式继续工作。非 PBR 材质保留原有的实时光照、描边与高清导出路径，不受残留光追或 AO 设置影响。玻璃和强反射场景可能仍需更高采样质量；这些材质用于视觉表达，并不代表原子真实光学性质。

采样质量提供“草稿、标准、高、超高”。标准档为 128 次预览采样和 256 次导出采样；超高档为 512 次预览采样和 2048 次导出采样，并将反弹上限提高到 12 次。超高不会自动启用，也不会绕过图像和几何上限，适合愿意等待更长时间的最终出图。

## 检查与安装更新

本地版可在“设置”中的“检查更新”查询官方发布。对于通过安装器管理的 `uv tool` 安装，发现新版本后可确认覆盖当前安装：先保存当前工作区，再下载并校验官方安装包，备份现有工具环境，覆盖安装并在原端口重启。只有新服务通过健康和版本检查后才刷新页面；安装或启动失败时尝试恢复上一份安装。

更新只修改当前 CrystalSketch 工具环境与入口，不修改原始结构文件或其他工具。更新过程中请等待状态提示，不要重复启动实例或关闭计算机。源码开发、非受控服务、多实例占用和在线版不支持页面内覆盖安装；在线版由站点发布更新，源码版按开发流程更新。

## 卸载

先在运行 CrystalSketch 的终端按 `Ctrl+C` 停止程序，再使用安装时的系统账号在终端执行：

```sh
uv tool uninstall crystalsketch
```

## 常见问题

### Windows 提示无法识别 Crystal

如果已经安装 CrystalSketch，但输入 `Crystal` 时提示“无法识别”或“不是内部或外部命令”，可能是命令目录尚未加入 PATH，或当前终端尚未刷新环境变量。

新版安装器已自动处理持久化 PATH。旧版用户可以下载最新压缩包并重新运行安装器，或在解压后的文件夹中仅修复命令路径，不重新安装程序：

```powershell
powershell -ExecutionPolicy Bypass -File .\install.ps1 -RepairPath
```

在 **PowerShell** 中完整复制并执行一次以下持久化修复命令：

```powershell
& {
    $ErrorActionPreference = 'Stop'
    $crystalBin = (uv tool dir --bin).Trim()

    if (-not (Test-Path -LiteralPath (Join-Path $crystalBin 'Crystal.exe'))) {
        throw '没有找到 Crystal.exe，请先确认 CrystalSketch 已安装。'
    }

    $userPaths = @(
        [Environment]::GetEnvironmentVariable('Path', 'User') -split ';' |
        Where-Object { $_ }
    )

    if ($userPaths -notcontains $crystalBin) {
        $userPaths = @($crystalBin) + $userPaths
    }

    [Environment]::SetEnvironmentVariable('Path', ($userPaths -join ';'), 'User')
    $env:Path = "$crystalBin;$env:Path"

    Crystal
}
```

这段命令会自动读取本机的命令目录，将其持久保存到当前用户的 PATH，同时刷新当前窗口并启动 CrystalSketch。它会保留已有的用户 PATH 条目，无需管理员权限，也无需手动填写用户名、盘符或安装路径。

修复后，日常直接输入 `Crystal` 即可，不需要每次重新执行修复。如果终端仍沿用旧环境，请完全退出 Windows Terminal 后重新打开，而不只是新建标签页；仍未刷新时，可注销并重新登录 Windows 一次。

此命令用于修复已安装程序的命令路径，不能替代安装。如果 `uv` 也无法识别，或 `uv tool list` 中没有 `crystalsketch`，请先按上面的安装步骤完成安装。

### Windows 提示无法识别 uv

先完全关闭终端程序并重新打开，再输入 `uv --version` 检查。如果尚未安装 uv，可在 PowerShell 中选择以下任意一种方式安装，**不需要两种都执行**。

方式一：使用 WinGet：

```powershell
winget install --id astral-sh.uv -e --source winget
```

方式二：如果无法识别 `winget`，使用官方安装脚本：

```powershell
powershell -ExecutionPolicy Bypass -Command "irm https://astral.sh/uv/install.ps1 | iex"
```

安装完成后，完全关闭终端程序并重新打开，再执行：

```powershell
uv --version
```

显示版本号即表示安装成功。

## 许可证

本项目采用 [MIT 许可证](LICENSE)。
